<UserControl x:Class="PACS.Views.AutoDiagnoseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PACS.Views"
             xmlns:prism="http://prismlibrary.com/" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:PACS.ViewModels"
             d:DataContext="{d:DesignInstance Type={x:Type viewmodels:AutoDiagnoseViewModel}}"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <materialDesign:DialogHost>
            <Border Margin="15" BorderBrush="Gray" BorderThickness="2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Margin="10" BorderBrush="Gray" BorderThickness="2">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Background="AliceBlue" Margin="10">图片的基本信息，相关参数</TextBlock>
                            <Button Grid.Row="1" Margin="10" Style="{DynamicResource MaterialDesignFlatButton}" Command="{Binding UploadFileCommand}" 
>上传图片</Button>
                            <Button Grid.Row="2" Margin="10"
                                Command="{x:Static materialDesign:DialogHost.OpenDialogCommand}"
                                Style="{DynamicResource MaterialDesignFlatButton}" Background="White" Cursor="Hand">
                                <Button.CommandParameter>
                                    <StackPanel Margin="16" Width="400" >
                                        <TextBlock HorizontalAlignment="Center" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Text="文件夹名" />

                                        <TextBox x:Name="FileFolder" Margin="0,50,0,0" HorizontalAlignment="Stretch" />
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button Style="{StaticResource MaterialDesignFlatButton}" IsDefault="True" Margin="0,8,8,0" Content="确认" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                                                     Click="CreateFolder">


                                            </Button>
                                            <Button IsCancel="True" Margin="0,8,8,0" Content="取消" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">
                                                <Button.Style>
                                                    <Style TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignFlatButton}">
                                                        <Setter Property="Foreground" Value="Red" />
                                                    </Style>
                                                </Button.Style>


                                            </Button>
                                        </StackPanel>
                                    </StackPanel>
                                </Button.CommandParameter>
                                保存自动诊断结果
                            </Button>
                        </Grid>

                    </Border>
                    <Border Grid.Column="1" Margin="10" >
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Margin="0 0 10 0" BorderBrush="Gray" BorderThickness="2">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <Grid Grid.Row="0" Margin="10">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="auto"/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0"   TextAlignment="Center" FontSize="20">原图</TextBlock>
                                        <Image Grid.Row="1"  Source="{Binding AutoDiagnoseItem.Image}"  HorizontalAlignment="Center" VerticalAlignment="Center" MaxHeight="200" MaxWidth="300" >
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="MouseLeftButtonUp">
                                                    <i:InvokeCommandAction Command="{Binding OriginalImageCommand}" 
                                                       CommandParameter="{Binding AutoDiagnoseItem.Image}" />
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </Image>
                                    </Grid>
                                    <Grid Grid.Row="1" Margin="10">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="auto"/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0"   TextAlignment="Center" FontSize="20">热力图</TextBlock>
                                        <Image Grid.Row="1"  Source="{Binding AutoDiagnoseItem.ThermodynamicChart}"  HorizontalAlignment="Center" VerticalAlignment="Center" MaxHeight="200" MaxWidth="300" >
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="MouseLeftButtonUp">
                                                    <i:InvokeCommandAction Command="{Binding OriginalImageCommand}" 
                                                       CommandParameter="{Binding AutoDiagnoseItem.ThermodynamicChart}" />
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </Image>
                                    </Grid>

                                </Grid>
                            </Border>
                            <Border Grid.Column="1" Margin="10 0 0 0" BorderBrush="Gray" BorderThickness="2">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <Grid Grid.Row="0" Margin="10">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="auto"/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0"   TextAlignment="Center" FontSize="20">标记骨折部位图</TextBlock>
                                        <Image Grid.Row="1"  Source="{Binding AutoDiagnoseItem.LabelImage}"  HorizontalAlignment="Center" VerticalAlignment="Center" MaxHeight="200" MaxWidth="300" >
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="MouseLeftButtonUp">
                                                    <i:InvokeCommandAction Command="{Binding OriginalImageCommand}" 
                                                       CommandParameter="{Binding AutoDiagnoseItem.LabelImage}" />
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </Image>
                                    </Grid>
                                    <Grid Grid.Row="1" Margin="10">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="auto"/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0"   TextAlignment="Center" FontSize="20">诊断结果分析</TextBlock>
                                        <TextBlock Grid.Row="1"  Text="{Binding AutoDiagnoseItem.DiagnoseContent}"  HorizontalAlignment="Center" VerticalAlignment="Center" MaxHeight="200" MaxWidth="300" />
                                    </Grid>
                                </Grid>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </materialDesign:DialogHost>
    </Grid>
</UserControl>
